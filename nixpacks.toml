[variables]
NODE_VERSION = "18"

[phases.setup]
nixPkgs = ["nodejs"]

[phases.install]
cmds = [
  "cd backend",
  "npm install"
]

[phases.build]
cmds = []

[phases.start]
cmd = "cd backend && npm start"